Run `/opt/eruun-load/simulate.py` and save its timing report to
`/app/load-timing.json` and `/logs/artifacts/load-timing.json`.

This is a synthetic task. It does not call a model or perform useful work.
