Create `/app/greeting.txt` containing exactly `Hello, Eruun!` followed by one newline.

Also create `/logs/artifacts/greeting.txt` with the same contents so the output
demonstrates preservation of a user artifact alongside Harbor's logs and rewards.
